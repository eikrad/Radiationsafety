# LLM provider: gemini or mistral
LLM_PROVIDER=mistral

# For Gemini (if LLM_PROVIDER=gemini)
# GOOGLE_API_KEY=your_google_api_key

# For Mistral (if LLM_PROVIDER=mistral)
# MISTRAL_API_KEY=your_mistral_api_key

# Optional Brave Search fallback when retrieval fails
WEB_SEARCH_ENABLED=false
# BRAVE_SEARCH_API_KEY=your_brave_search_api_key

# LangSmith tracing (disable when using API keys from frontend Settings)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_PROJECT=radiation-safety-rag
# LANGCHAIN_API_KEY=your_langsmith_api_key
# When using API keys from frontend: tracing is auto-disabled for those requests
