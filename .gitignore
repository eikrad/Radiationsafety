# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Chroma vector DB
.chroma/

# Environment
.env

# Log files (runtime, debug – use 'git add -f' if you ever need to track one)
*.log

# Eval report outputs (optional: remove this line to commit reports)
eval/reports/

# LibreOffice lock files (do not commit)
.~lock.*

# Document registry (generated or from example; remove this line to commit a shared registry)
document_sources.yaml
